# 🎯 Number Guessing Game (Python)

This is a simple Number Guessing Game built using Python.  
The program randomly generates a number between **1 and 100**, and the player must guess the correct number.  
After each guess, the program gives hints and counts the number of attempts. The player can choose to play again after winning.

---

## 📌 Features

- Generates a random number between 1 and 100
- Gives hints if the guess is too high or too low
- Counts the number of attempts
- Option to play again after guessing correctly
- Handles invalid (non-numeric) input using exception handling

---

## 🛠️ Technologies Used

- Python 3
- random module

---

## 📂 Project Structure

